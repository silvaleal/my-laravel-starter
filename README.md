# my-laravel-starter
Kit inicial para usar o laravel 12, uso breeze e pinguim-ui (tailwind + alpinejs)

# Preparando o ambiente
```
composer install
cp .env.example .env
php artisan key:generate
npm install
npm run dev
```